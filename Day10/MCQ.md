
- In order for a class to become an abstract class in python, what class should it inherit
a. AbstractMethod b. ABC c. abc d. abstractmethod

- In order for a class to become an abstract class in python, what class should it inherit
a. AbstractMethod b. ABC c. abc d. abstractmethod

- Which of the following imports is false
a. from abc import ABC b. import pandas as pd c. import abc from ABC d. import pendulumn

- when a variable or a method starts with single underscore, what does it mean?
a. private b. public c. protected d. invalid

- Which of the following is true about runtime polymorphism
a. overriding method name in child class can be different
b. overriding method name in both parent and child class should be the same
c. None of the above








- On which count of an object, Garbage Collector clean up the memory
a. 10  b. 1 c.-1 d.0

- Which of the pattern is used for matching two incompatible class types
a. Factory pattern b. Singleton c. Adapter d. Strategy
